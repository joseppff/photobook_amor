// Los tipos ahora se importan desde el backend
export type { PhotoPage, YearData, PhotobookData } from "@/backend/types";

// Los datos ahora se cargan desde el backend mediante la API
// Ver src/lib/api.ts para las funciones de carga de datos

